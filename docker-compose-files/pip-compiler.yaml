version: "3.4"

services:
  pip-compiler:
    build:
      context: ../docker-files/pip-compiler
    environment:
      IN_REQUIREMENTS: "${IN_REQUIREMENTS}"
      SRC_APP: "${SRC_APP}"
    volumes:
      - "../:/usr/src/app"
